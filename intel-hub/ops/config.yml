# 配置文件模板
# database:
#   path: # SQLite数据库路径
# collector:
#   interval: # 采集间隔（秒）
#   timeout: # 请求超时（秒）
# notifier:
#   enabled: # 是否启用推送
# web:
#   host: # Web服务地址
#   port: # Web服务端口
# 顶层（推荐）👇


# 自检开关（顶层）👇